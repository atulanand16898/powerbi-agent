# Copy this to secrets.toml for local Streamlit use,
# OR paste these values into Streamlit Cloud → App settings → Secrets

ANTHROPIC_API_KEY = "sk-ant-..."

# Power BI (optional)
POWERBI_AUTH = "password"        # or "device" or "app"
POWERBI_USERNAME = "you@org.com"
POWERBI_PASSWORD = "yourpassword"
